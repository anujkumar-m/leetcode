# Write your MySQL query statement below
select e.name, b.bonus from(select empId,name from Employee) e left join(select empId,bonus from Bonus) b on e.empId = b.empId where b.bonus < 1000 or bonus is null;